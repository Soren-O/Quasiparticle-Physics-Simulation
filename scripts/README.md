# scripts/

User-facing run scripts. One-to-one with `validation/` entries when useful,
plus ad-hoc analysis scripts that live outside the audit suite (the
`run_prelim_*` sweeps are one-off prelim-exam analysis runs; their
committed outputs are historical). See New Framework Plan §5.
