# scripts/

User-facing run scripts. One-to-one with `validation/` entries when useful,
plus ad-hoc analysis scripts that live outside the audit suite.

Populated during Gate 2+ as backends come online. See New Framework Plan §5.
