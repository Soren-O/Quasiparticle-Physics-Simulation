# Material Database

**Status:** stub. Gate 1 skeleton.

This document will describe the `Material` dataclass, the `Substrate`
descriptor, the YAML loader under `qpsim/materials/data/`, and how to
add a new material (including the substrate-transmission coefficient η
used in the acoustic-escape τ_l form).

Until it is written, the authoritative reference is
`New Framework Plan.md` §4.5. For τ_l(ω), see
`docs/Phonon_Escape_Time.md`.
